#include <iostream>
#include <string>

int pangramCheck(string &sentence)
{
	//longest (sensible) pangram I could find on Google was 51 characters. Perhaps a creative linguist coukd make one longer, but not too much so.
	char sentenceChars[64];
}
